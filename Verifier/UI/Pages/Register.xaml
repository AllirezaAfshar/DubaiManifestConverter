<template:DetailsPage x:Class="Verifier.UI.Pages.Register"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:template="clr-namespace:Verifier.Template"
      mc:Ignorable="d">
    <StackPanel FlowDirection="RightToLeft">
        <TextBlock TextWrapping="Wrap" TextAlignment="Justify">در صورتی که قبلا در سامانه ثبت نام نکرده اید برای ورود بایستی نام کاربری خود را در قسمت زیر وارد نمایید. در صورتی که نام کاربری وارد شده قبلا در سامانه ثبت نشده باشد صفحه ای بازخواهد شد که در آن می توانید رمزعبور خود را وارد نمایید. با ترکیب نام کاربری و رمزعبور می توانید در سامانه وارد شوید پس در حفظ و نگهداری نام کاربری و رمزعبور خود کوشا باشید.</TextBlock>
        <TextBlock></TextBlock>
        <DockPanel HorizontalAlignment="Stretch">
            <TextBlock DockPanel.Dock="Left">نام کاربری</TextBlock>
            <TextBox Name="txtUsername" HorizontalAlignment="Stretch"></TextBox>
        </DockPanel>
        <TextBlock></TextBlock>
        <Button Name="btnRegister" Click="BtnRegister_OnClick">ثبت نام</Button>
        <Button Name="btnLogin" Click="BtnLogin_OnClick">ورود</Button>
    </StackPanel>
</template:DetailsPage>
