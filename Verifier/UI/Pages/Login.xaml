<template:DetailsPage x:Class="Verifier.UI.Pages.Login"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:template="clr-namespace:Verifier.Template"
      mc:Ignorable="d">
    <StackPanel Orientation="Vertical" FlowDirection="RightToLeft" HorizontalAlignment="Stretch">
        <TextBlock TextWrapping="Wrap" TextAlignment="Justify">
            به منظور استفاده از سامانه باید وارد شوید. 
        </TextBlock>
        <TextBlock TextWrapping="Wrap" TextAlignment="Justify">
            در صورتی که اولین بار است از سامانه استفاده می کند ابتدا باید ثبت نام کنید.
        </TextBlock>
        <TextBlock></TextBlock>
        <TextBlock></TextBlock>
        <DockPanel HorizontalAlignment="Stretch">
            <TextBlock DockPanel.Dock="Left">نام کاربری  </TextBlock>
            <TextBox Name="txtUsername" HorizontalAlignment="Stretch"></TextBox>
        </DockPanel>
        <TextBlock></TextBlock>
        <Button Name="btnLogin" Click="BtnLogin_OnClick"> ورود </Button>
        <Button Name="btnRegister" Click="BtnRegister_OnClick"> ثبت نام </Button>
    </StackPanel>
</template:DetailsPage>
